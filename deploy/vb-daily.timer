[Unit]
Description=Run vb_data daily update at 01:00 local time

[Timer]
OnCalendar=*-*-* 01:00:00
Persistent=true
# Small jitter so we are not hammering the site exactly on the hour.
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
